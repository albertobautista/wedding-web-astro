---
const { isDark } = Astro.props;
---

<div class="flex items-center justify-center">
  <div class={`w-20 h-px ${isDark ? "bg-white" : "bg-gray-400"}`}></div>
  <div class="relative mx-1">
    <div class="p-2 rounded-full icon">
      <slot />
    </div>
  </div>
  <div class={`w-20 h-px ${isDark ? "bg-white" : "bg-gray-400"}`}></div>
</div>
